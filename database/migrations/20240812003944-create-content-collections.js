/* eslint-disable @typescript-eslint/no-unused-vars */
// REMOVE ABOVE LINE WHEN DONE
module.exports = {
  async up(db, client) {
    // READ FROM app/_configs/_schema and generate collections with schema validation for each one.
  },

  async down(db, client) {
    // DROP ALL content collections
  },
};
