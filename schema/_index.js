/* eslint-disable @typescript-eslint/no-var-requires */
const weddings = require('./weddings.js');
const films = require('./films.js');
const flowers = require('./flowers.js');

const schema = { weddings, films, flowers };

module.exports = schema;
